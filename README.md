# Application to try with Kpack

## Initialize the project

```bash
uv init kpack-fastapi-bookshop
```

```bash
gh repo create kpack-fastapi-bookshop --public
git remote add origin https://github.com/KAMRON-DEVELOPER/kpack-fastapi-bookshop.git
git branch -M master
git push -u origin master
```

```bash
uv add fastapi pydantic uvicorn 
```
