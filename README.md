# Poddle fastapi bookshop sample application

## Initialize the project

```bash
uv init pack-fastapi-bookshop
```

```bash
gh repo create pack-fastapi-bookshop --public
git remote add origin https://github.com/KAMRON-DEVELOPER/pack-fastapi-bookshop.git
git branch -M master
git push -u origin master
```

```bash
uv add fastapi pydantic uvicorn 
```
